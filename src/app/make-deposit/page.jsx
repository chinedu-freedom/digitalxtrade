import MakeDepositPage from '../deposit/page';

export const metadata = {
  title: 'Make a Deposit - DigitalXTrade',
  description: 'Select an investment package and deposit crypto to earn daily automated returns.',
};

export default function MakeDepositAlias() {
  return <MakeDepositPage />;
}
